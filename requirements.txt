fastapi==0.115.12
uvicorn[standard]==0.34.2
sqlalchemy==2.0.40
alembic==1.15.2
psycopg[binary]==3.2.6
pandas==2.2.3
rapidfuzz==3.13.0
stripe==12.0.0
jinja2==3.1.6
python-multipart==0.0.20
playwright==1.54.0
dnspython==2.7.0
google-auth==2.40.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.149.0
httpx==0.27.2
trafilatura==1.12.2
# AI personalization (DESIGN_V2 Phase 3A). Only used when PERSONALIZATION_ENABLED
# + ANTHROPIC_API_KEY; imported lazily so the app runs without it, pinned so
# enabling the flag just works.
anthropic==0.39.0
# Optional worker queue (only needed if DISCOVERY_USE_RQ=true + REDIS_URL set):
# redis==5.2.1
# rq==2.1.0
